Telegram бот для управления файлами и процессами на хостинге с ограниченными ресурсами. Позволяет загружать, запускать и останавливать Python скрипты, а также мониторить состояние сервера.

ВОЗМОЖНОСТИ:

Загрузка .py и .sh файлов через Telegram

Запуск загруженных файлов с низким приоритетом

Остановка запущенных процессов

Мониторинг состояния RAM и CPU

Просмотр всех запущенных Python процессов

Проверка свободной памяти перед запуском

Ограничение на количество запущенных процессов

ТРЕБОВАНИЯ:

Python 3.7 или выше

100+ MB свободной RAM

Telegram Bot Token от @BotFather

ЗАВИСИМОСТИ:
aiogram==3.0.0
psutil==5.9.5

УСТАНОВКА:

Установите зависимости:
pip install aiogram==3.0.0 psutil==5.9.5

Настройте конфигурацию в файле bot.py:
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE" - вставьте токен от @BotFather
ADMIN_IDS = [123456789, 987654321] - вставьте свои Telegram ID

Запустите бота:
python bot.py

НАСТРОЙКА:

Основные переменные в коде:
BOT_TOKEN - токен Telegram бота (обязательно)
ADMIN_IDS - список ID администраторов
ALLOWED_EXTENSIONS - разрешенные расширения файлов (.py, .sh)
MAX_FILE_SIZE - максимальный размер файла (10 MB)

Как получить свой Telegram ID:

Напишите боту @userinfobot

Нажмите Start

Бот покажет ваш ID

КОМАНДЫ:

Основные команды:
/start - главное меню с кнопками
/status - статус RAM и CPU
/stop [PID] - остановить процесс по PID

Кнопки меню:
📁 Загрузить - инструкция по загрузке файлов
📋 Файлы - список доступных .py/.sh файлов
🔄 Процессы - просмотр запущенных процессов
📊 Статус - информация о памяти и CPU
❌ Остановить - выбор процесса для остановки

ИСПОЛЬЗОВАНИЕ:

Как загрузить и запустить файл:

Через меню:

Нажмите кнопку "📁 Загрузить"

Отправьте боту .py файл

Файл автоматически сохранится на сервере

Для запуска нажмите "📋 Файлы" и выберите нужный файл

Как остановить процесс:

Нажмите "❌ Остановить"

Выберите процесс из списка

Подтвердите остановку

Как проверить статус:
Введите команду /status
Бот покажет использование RAM в процентах и свободную память в MB

УПРАВЛЕНИЕ ПАМЯТЬЮ:

Бот оптимизирован для работы с ограниченными ресурсами:

Проверка памяти перед запуском скриптов (требуется минимум 200 MB свободно)

Запуск с низким приоритетом через nice на Linux

Ограничение на количество процессов (максимум 10)

Принудительный сбор мусора после операций

Минимальное логирование для экономии памяти

БЕЗОПАСНОСТЬ:

Доступ только для администраторов из списка ADMIN_IDS

Только .py и .sh файлы разрешены для загрузки

Ограничение размера файла (10 MB)

Проверка свободной памяти перед запуском

УСТРАНЕНИЕ ПРОБЛЕМ:

Ошибка "Недостаточно памяти":

Остановите другие процессы командой /stop

Проверьте свободную память через /status

Перезапустите сервер если необходимо

Бот не отвечает:

Проверьте что бот запущен

Проверьте токен в конфигурации

Проверьте логи на наличие ошибок

Файл не загружается:

Проверьте расширение файла (только .py или .sh)

Проверьте размер файла (максимум 10 MB)

Убедитесь что вы администратор

Процесс не запускается:

Проверьте свободную память (нужно минимум 200 MB)

Проверьте что файл существует и имеет правильные права доступа

Проверьте синтаксис Python файла

АВТОР: Разработано для управления хостингом Pterodactyl

ЛИЦЕНЗИЯ: MIT
